from django.contrib.auth.models import User

✅ Authentication system (in simple words)

The authentication system is the part of Django that:

✔ verifies who the user is
✔ allows login and logout
✔ manages passwords
✔ controls access (who can see what)

It answers the question:

👉 “Are you really this user?”

