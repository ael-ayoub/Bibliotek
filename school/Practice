null → database

blank → validation

unique → uniqueness rule

default → automatic value

primary_key → identifies the row

choices → limits allowed value


7️⃣ on_delete (Important)

What happens if related object deleted?

Options:

Option	Meaning
CASCADE	delete related
PROTECT	prevent delete
SET_NULL	set to null
DO_NOTHING	do nothing